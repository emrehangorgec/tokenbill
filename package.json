{
  "name": "tokenbill",
  "version": "0.2.0",
  "description": "Where did my AI budget go? Token cost explainer for Claude Code sessions.",
  "type": "module",
  "bin": { "tokenbill": "dist/cli.js" },
  "main": "dist/cli.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "tsx src/cli.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "tsx": "^4.16.0",
    "@types/node": "^22.0.0"
  }
}
